@model IEnumerable<DepartmentDto>

<h1>All Departments</h1>
<a class="btn btnalert-primary my-3" asp-controller="Departments" asp-action="Create"> Create New Department </a>

<br />
@if (Model.Any())
{
 <table class="table table-striped table-hover mt-2">
     <thead class="text-center">
	    <tr>
		   <th>Code</th>
		   <th>Name</th>
		   <th>Description</th>
		   <th>Details</th>
		   <th>Update</th>
		   <th>Delete</th>
	    </tr>
     </thead>
	 <tbody> 
		 @foreach(var department in Model)
		 {
			 <tr>
				 <td>@department.Code</td>
				 <td>@department.Name</td>
				 <td>@department.Description</td>
                 <td>@department.DateOfCreation</td>
					<partial name="_ButtonPartialView" model="@department.DeptId" />
					@*<a class="btn btn-danger" asp-action="Delete" asp-all-route-id="@department.DeptId"><i class="fas fa-trash"></i>Deletes </a>*@
				 <td>
					<button type="button"
							class="btn btn-danger"
							data-bs-toggle="modal"
							data-bs-target="#DeleteModal-@department.DeptId">
						<i class="fas fa-trash"></i> Delete
					</button>
				 </td>
			 </tr>
		 }
	 </tbody>

</table>
}else
{
<div class="alert alert-info mt-2" >
<h2>No Departments Found !!</h2>
</div>
}